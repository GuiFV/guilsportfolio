version: '3.7'
services:
  guilsportfolio_web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./staticfiles:/app/staticfiles
      - .:/app
    ports:
      - "8001:8000"